name: Check spelling # cSpell:ignore refcache

on:
  pull_request:

jobs:
  check-formatting:
    name: Check spelling
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # cspell has no dependencies that need to be installed so we can skip this step
      # - run: npm install
      - run: npm run check:spelling
