{{ $entries := slice -}}
{{ range $key, $entry := .Site.Data.registry -}}
  {{ $entry = merge $entry (dict "_key" $key) -}}
  {{ $entries = $entries | append $entry -}}
{{ end -}}
{{ jsonify (dict "indent" "  ") $entries -}}
